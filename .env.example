# Backend
RESOLVEAI_DB_PATH=./backend/resolveai.db
RESOLVEAI_CORS_ORIGINS=http://localhost:3000

# Optional Gemini integration. Leave blank to use the deterministic fallback.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
